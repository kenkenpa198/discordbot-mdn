# Docker イメージのビルドを定義する
build:
  # Dockerfile の指定
  docker:
    worker: Dockerfile
  # .env に記載している環境変数（値は ホストOS から）を Dockerfile へ渡す
  config:
    BOT_TOKEN: $BOT_TOKEN

# プログラムを実行する
run:
  worker: python3 -u mdn.py # -u: Python のログを表示するようにする